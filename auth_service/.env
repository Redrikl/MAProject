# auth_service/.env — пример, положи этот файл в папку auth_service/
# -----------------------------------------
# Переменные окружения для запуска auth_service в docker-compose
# Настроены под сервис db в docker-compose (hostname = db)

# Flask / runtime
FLASK_ENV=production
PORT=5000
SECRET_KEY=change-me-to-a-strong-secret

# Database (Postgres inside docker-compose)
# Формат: postgresql://<user>:<password>@<host>:<port>/<database>
DATABASE_URL=postgresql://exam:exam@db:5432/user_platform

# Optional (логгирование, режимы и т.п.)
LOG_LEVEL=info

# Alembic (не обязательно, alembic.env.py прочитает DATABASE_URL из окружения)
# If you want a separate value for alembic, uncomment and set:
# ALEMBIC_DATABASE_URL=${DATABASE_URL}
